<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.member.dao.MemberDAO">

	<select id="printRandom" resultType="ListDTO">
  <![CDATA[
  SELECT * FROM 
    (SELECT * FROM movie ORDER BY dbms_random.value)
  WHERE ROWNUM BETWEEN 0 AND 4
  ]]>
  
  
    </select>
    
    <!-- 회원가입 -->
	<insert id = "insertMember" parameterType = "MemberDTO">
		<![CDATA[
			INSERT INTO member(email, password, name, age, gender, memberType)
			VALUES (#{email}, #{password}, #{name}, #{age}, #{gender}, #{memberType})
		]]>
	</insert>
	
	<!-- 회원정보 가져오기 -->
	<select id = "selectByEmail" parameterType = "string" resultType="MemberDTO">
		SELECT * FROM member
		WHERE email = #{email}
	</select>
	
	
	<!-- 회원 정보 수정 -->
	<update id ="updateMember" parameterType ="MemberDTO">
		UPDATE member
		SET  password=#{password}, name=#{name}, memberPhone=#{memberPhone}
		WHERE memberEmail=#{memberEmail}	
	</update>
	
	<!-- 비밀번호 변경 -->
	<update id="updateByPass" parameterType="MemberDTO">
		UPDATE member SET memberPass=#{memberPass}
		WHERE memberEmail=#{memberEmail}
	</update>
</mapper>