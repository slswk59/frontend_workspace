<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- !DOCTYPE mapper는 루트요소를 mapper로 하라는 의미 -->
    
<mapper namespace="com.example.shop.members.dao.MembersDAO">

  <!-- 회원가입 -->
  <insert id="insertMember" parameterType="MembersDTO">
    <![CDATA[
        INSERT INTO members(memberEmail, memberPass, memberName, memberPhone, memberType, rememberEmail, profileImage)
        VALUES(
            #{memberEmail},
            #{memberPass},
            #{memberName},
            #{memberPhone},
            #{memberType},
            #{rememberEmail},
            <if test="profileImage != null">
                #{profileImage}
            </if>
        )
        ON DUPLICATE KEY UPDATE
            memberPass = #{memberPass},
            memberName = #{memberName},
            memberPhone = #{memberPhone},
            profileImage = 
            <if test="profileImage != null">
                #{profileImage}
            </if>
    ]]>
</insert>

  
  <!-- memberEmail은 중복되지 않은 고유의 값이므로, 해당 Email에 해당하는 member정보를 가져오라는 의미 -->
  <select id="selectByEmail" parameterType="string" resultType="MembersDTO">
  	SELECT * FROM members
  	WHERE memberEmail = #{memberEmail}
  </select>
  
  <!-- 회원 정보 수정 (비밀번호, 이름, 전화번호) -->
  <update id="updateMember" parameterType="MembersDTO">
  	UPDATE members
  	SET memberPass=#{memberPass}, memberName=#{memberName}, memberPhone=#{memberPhone}
  	WHERE memberEmail=#{memberEmail}
  </update>
  
  <!-- 비밀번호 변경 -->
  <update id="updateByPass" parameterType="MembersDTO">
  	UPDATE members
  	SET memberPass=#{memberPass}
  	WHERE memberEmail=#{memberEmail}
  </update>
  
  <update id="updateProfileImage" parameterType="MembersDTO">
    update members
    <if test="profileImage != null">
        set profileImage = #{profileImage}
    </if>
    where memberEmail = #{memberEmail}
  </update>

</mapper>